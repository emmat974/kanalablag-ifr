{% extends 'base.html.twig' %}

{% block title %}Tous les meilleurs blagues réunionnaises{% endblock %}

{% block body %}

<div class="row my-2">
    <div class="col-md-12">
        <ul class="nav justify-content-center">
            {% for category in categories %}
            <li class="nav-item">
                <a class="btn btn-dark mx-2" href="{{ path("app_home_categories", {id: category.id}) }}">{{ category.name }}</a>
            </li>
            {% endfor %}
        </ul>
    </div>
</div>

{% include "home/show_jokes.html.twig" with {jokes: jokes} %}

{% endblock %}


