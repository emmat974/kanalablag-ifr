{% extends 'base.html.twig' %}

{% block title %}Joke{% endblock %}

{% block body %}
    <h1>Joke</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joke.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ joke.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ joke.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ joke.createdAt ? joke.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ joke.updatedAt ? joke.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_joke_index') }}">back to list</a>

    <a href="{{ path('app_joke_edit', {'id': joke.id}) }}">edit</a>

    {{ include('joke/_delete_form.html.twig') }}
{% endblock %}
